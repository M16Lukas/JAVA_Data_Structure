package ch4_abstract_interface.abstractEX;

/**
 * Main
 */
public class Main {
  // 추상 클래스(abstract class)

  // 1. 추상 메서드 : 선언만 있고 구현이 없는 메서드
  // 2. 추상 클래스 : 추상 메서드를 포함한 클래스
  // 3. 키워드는 abstract 로 표시
  // 4. 추상 클래스는 객체를 만들수없음. 따라서 서브 클래스를 만드는 용도로만 사용됨
}